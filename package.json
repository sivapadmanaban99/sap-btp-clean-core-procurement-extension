{
  "name": "@ai-clean-core-poc/main",
  "version": "0.0.1",
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "deploy:sqlite": "cds deploy --to sqlite:db.sqlite",
    "deploy:hana": "cds deploy --to hana",
    "test": "echo \"(tests later)\"",
    "watch-materialforecast_fe": "cds watch --open sap.ai.clean.core.ui.materialforecastfe/index.html?sap-ui-xx-viewCache=false --livereload false"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.0.3",
    "@sap/cds": "^9.3.1",
    "@sap/cds-dk": "*",
    "cds-plugin-ui5": "^0.13.0"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite"
      }
    }
  },
  "dependencies": {
    "@sap/cds": "^6.8.2"
  },
  "workspaces": [
    "app/*"
  ],
  "sapux": [
    "app/materialforecast_fe"
  ]
}
